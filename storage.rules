
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read, write, and delete for any authenticated user.
    // This is suitable for an internal app where all users are trusted.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
